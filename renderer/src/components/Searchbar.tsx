import React, { Component } from "react";

export class Searchbar extends Component {
  render() {
    return (
      <header>
        <p>Search</p>
      </header>
    );
  }
}

export default Searchbar;
